.model small
.stack 100h
.data
    msg db 'Input a character: $'
.code
main proc
    mov ax, @data
    mov ds, ax

    ; ?. ????? ?????? ????? ???????
    mov dx, offset msg
    mov ah, 09h
    int 21h

    ; ?. ??????? ???? ????? ?????
    mov ah, 01h
    int 21h
    mov bl, al          ; ????? ????? ????? bl ?????????? ??? ??? ???

    ; --- Print HIGH nibble (????? ????? ?????) ---
    mov al, bl
    shr al, 4           ; ????? ? ??? ????? ???
    call print_hex

    ; --- Print LOW nibble (??????? ????? ?????) ---
    mov al, bl
    and al, 0Fh         ; ????? ? ??? ????? ???
    call print_hex

    ; Exit (????????? ???)
    mov ah, 4Ch
    int 21h
main endp

; ---------------------------------
; ????? ????? ??????? ???? ????? (???? ????)
; ---------------------------------
print_hex proc
    add al, 48
    cmp al, 57
    jbe out_label
    add al, 7
out_label:
    mov dl, al
    mov ah, 02h
    int 21h
    ret
print_hex endp

end main